---
phase: 04-firebase-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/migrate-drop-firebase-id.sql
autonomous: false
requirements: [CLEAN-01, CLEAN-03]

must_haves:
  truths:
    - "Firebase Cloud Functions (onItemCreated/Updated/Deleted) are no longer deployed and Firestore changes do NOT sync to Supabase"
    - "orders 테이블에 firebase_id 컬럼이 존재하지 않는다"
  artifacts:
    - path: "scripts/migrate-drop-firebase-id.sql"
      provides: "ALTER TABLE orders DROP COLUMN firebase_id migration script"
      contains: "DROP COLUMN"
  key_links: []
---

<objective>
Firebase Cloud Functions 배포 해제 및 orders.firebase_id 컬럼 제거

Purpose: Firestore-to-Supabase 실시간 동기화를 완전히 중단하고, 마이그레이션 전용이었던 firebase_id 컬럼을 제거하여 Firebase 의존성의 데이터 계층을 정리한다.
Output: Cloud Functions 삭제 확인, firebase_id 컬럼 DROP SQL 스크립트
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-data-migration/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create firebase_id column DROP migration script</name>
  <files>scripts/migrate-drop-firebase-id.sql</files>
  <read_first>
    - .claude/rules/db-schema.md
  </read_first>
  <action>
Create `scripts/migrate-drop-firebase-id.sql` with the following SQL:

1. First, drop the UNIQUE index on firebase_id (if it exists as a named constraint):
   ```sql
   ALTER TABLE orders DROP CONSTRAINT IF EXISTS orders_firebase_id_key;
   ```
2. Then drop the column:
   ```sql
   ALTER TABLE orders DROP COLUMN IF EXISTS firebase_id;
   ```
3. Add a verification query at the end (commented, for manual confirmation):
   ```sql
   -- Verify: should return 0 rows
   -- SELECT column_name FROM information_schema.columns WHERE table_name = 'orders' AND column_name = 'firebase_id';
   ```

This is a migration script to be executed in Supabase SQL Editor (consistent with project convention in `scripts/` directory). The `IF EXISTS` guards make it safe to re-run.

Note: firebase_id was only used for Firebase-to-Supabase sync (Cloud Functions upsert on firebase_id) and migration deduplication. With Phase 3 complete and all 9,697 records migrated using Supabase's own `id` as primary key, firebase_id serves no further purpose.
  </action>
  <verify>
    <automated>test -f scripts/migrate-drop-firebase-id.sql && grep -q "DROP COLUMN" scripts/migrate-drop-firebase-id.sql && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/migrate-drop-firebase-id.sql exists
    - Contains ALTER TABLE orders DROP COLUMN firebase_id
    - Contains IF EXISTS guard for safe re-run
  </acceptance_criteria>
  <done>Migration script exists with DROP COLUMN firebase_id and IF EXISTS safety guard</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Decommission Firebase Cloud Functions and execute firebase_id migration</name>
  <files>scripts/migrate-drop-firebase-id.sql</files>
  <action>
This is a human-action checkpoint. The user must perform 3 manual steps that cannot be automated by Claude (Firebase Console access and Supabase SQL Editor execution).

**Step A: Delete Cloud Functions (Firebase Console)**
1. Go to https://console.firebase.google.com/project/transactionledger-3f134/functions
2. Delete all 3 functions: `onItemCreated`, `onItemUpdated`, `onItemDeleted`
3. Confirm they no longer appear in the Functions list

OR via CLI (if firebase-tools is installed):
```bash
firebase functions:delete onItemCreated onItemUpdated onItemDeleted --project transactionledger-3f134 --force
```

**Step B: Execute firebase_id column DROP**
1. Open Supabase SQL Editor (https://supabase.com/dashboard/project/YOUR_PROJECT/sql)
2. Paste and run the contents of `scripts/migrate-drop-firebase-id.sql`
3. Verify by running: `SELECT column_name FROM information_schema.columns WHERE table_name = 'orders' AND column_name = 'firebase_id';`
   - Expected: 0 rows returned

**Step C: Confirm the app still loads orders correctly**
1. Visit the orders page and confirm the list loads without errors
  </action>
  <verify>
    <automated>echo "CHECKPOINT: User must confirm Cloud Functions deleted and firebase_id column dropped"</automated>
  </verify>
  <acceptance_criteria>
    - Cloud Functions are deleted from Firebase project transactionledger-3f134
    - firebase_id column no longer exists in orders table
    - Orders page loads correctly without firebase_id column
  </acceptance_criteria>
  <done>Cloud Functions decommissioned, firebase_id column dropped, orders page verified</done>
  <resume-signal>Type "done" after completing all steps, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
- Firebase Console shows 0 deployed Cloud Functions
- `SELECT column_name FROM information_schema.columns WHERE table_name = 'orders' AND column_name = 'firebase_id'` returns 0 rows
- Orders page loads without errors
</verification>

<success_criteria>
- Cloud Functions (onItemCreated/Updated/Deleted) deleted from Firebase project
- firebase_id column removed from orders table
- Application continues to function correctly
</success_criteria>

<output>
After completion, create `.planning/phases/04-firebase-cleanup/04-01-SUMMARY.md`
</output>
